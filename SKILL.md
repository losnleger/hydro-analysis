---
name: hydro-analysis
description: 面向 WorkBuddy 等 Agent 开发与运行的水文监测与分析 skill。用于洪水过程线推算、降雨-径流分析、SCS单位线法计算、专业水文可视化图表生成、完整分析报告输出。当用户提到"洪水过程线"、"降雨径流"、"SCS单位线"、"水文分析"、"汇流计算"时使用此技能。
---

# 面向 Agent 的水文分析专业技能 (Hydro-Analysis Skill)

## 概述

这是一个面向 WorkBuddy 等 Agent 开发与运行的 skill，用于描述和复用水文分析工作流，包括：
- 降雨-径流过程计算（SCS单位线法）
- 专业水文可视化图表生成
- 完整分析报告输出（Word + Excel + HTML）

当前开源仓库实际随附的是分析规范和一个参考计算脚本；可视化、Word、Excel
和 HTML 生成器尚未随包提供。请以仓库 README 中的能力边界和验证说明为准。

## 使用场景

当用户需要：
1. 推算出口断面的洪水过程线
2. 进行降雨-径流分析
3. 生成专业水文可视化图表
4. 输出完整的水文分析报告

## 核心计算方法

### SCS单位线法参数选取

| 参数 | 说明 | 推荐值 |
|------|------|--------|
| CN | 曲线数（反映流域特征） | 半干旱地区：70-80；湿润地区：80-90 |
| tc | 汇流时间 | 经验公式：tc = 0.0195 × L^0.77 × S^-0.385 × 60 (min) |
| tp | 滞后时间 | tp = tc/120 (h) 或经验值1.5-2.5h |
| Δt | 计算时段 | Δt = 0.133 × tp |

### 洪峰流量计算公式

```
Qp = 2.08 × A / tp
```
其中：
- Qp: 洪峰流量 (m³/s)
- A: 流域面积 (km²)
- tp: 滞后时间 (h)

## 可视化图表标准

### 降雨-径流双轴图规范

这是水文分析的核心可视化成果，必须严格遵循以下标准：

#### 图表结构

```
        时间轴(X轴) ────────────────────────  ← X轴在顶部
           ↓               ↓
总降雨  ██████████████████   ← 蓝色宽柱(40%宽度)
净雨量      ██████████       ← 橙色窄柱(20%宽度)，叠在蓝色内部居中
           ↓               ↓
        ────────────────────────────────  ← 基线
       ↗      ↗       ↗       ↗          ← 流量曲线（向上延伸）

流量(m³/s)                        时间轴(X轴) ─────────────────
```

#### 配色方案

| 要素 | 颜色代码 | 说明 |
|------|---------|------|
| 总降雨柱 | #3498db / #5B9BD5 | 蓝色，透明度0.75-0.8 |
| 净雨量柱 | #e67e22 / #ED7D31 | 橙色，透明度0.85-0.9 |
| 流量曲线 | #27ae60 / #70AD47 | 绿色，线宽2.5-3px |
| 背景色 | #ffffff | 纯白背景 |
| 网格线 | #f0f0f0 | 浅灰虚线 |

#### 图表要素

1. **双X轴设计**
   - 上方X轴：仅显示刻度（无标签和数值）
   - 下方X轴：显示刻度、数值、"时间(h)"标签

2. **双Y轴设计**
   - 左侧Y轴：降雨量（向下倒置，inverse: true）
   - 右侧Y轴：流量（向上）

3. **柱状图叠加**
   - 总降雨：宽柱（barWidth: 28-40px）
   - 净雨量：窄柱（barWidth: 14-20px）
   - **关键**：两个柱子必须同一X轴位置，净雨量柱居中叠在总降雨柱内部

4. **流量曲线**
   - 平滑曲线（smooth: true）
   - 可选：面积填充（areaStyle，透明度0.1-0.2）
   - 可选：洪峰标注（峰值点特殊标记）

#### 图表尺寸

- 推荐比例：16:9
- 推荐分辨率：200 dpi（打印用）
- 交互式图表：自适应容器

### 表格规范

#### 数据表格样式

| 要素 | 规范 |
|------|------|
| 表头 | 深色渐变背景（#34495e → #2c3e50），白色文字 |
| 时间列 | 浅灰背景（#ecf0f1），加粗 |
| 数据列 | 按类型配色（蓝/橙/绿） |
| 洪峰行 | 黄色背景（#fff3cd），加粗 |
| 悬停效果 | 浅蓝背景（#e8f4fc） |

#### 双列布局

将16个时段数据分为两列展示（0-7时、8-15时），提高可读性。

## 报告输出标准

### Word报告结构

```
1. 流域与降雨参数
   - 流域面积、径流系数、降雨时段
   - 总降雨量、总净雨量

2. 汇流参数
   - CN值、汇流时间、滞后时间
   - 洪峰流量系数

3. 洪水过程线数据
   - 完整时段数据表
   - 洪峰行高亮显示

4. 洪水特征汇总
   - 洪峰流量、峰现时间
   - 洪峰模数、涨退水历时
   - 洪水总量

5. 分析结论
   - 方法适用性说明
   - 洪水过程特征描述
   - 建议

6. 降雨-径流可视化图表
   - 交互式图表说明（HTML文件）
   - 静态图表（PNG，适合打印）

附录：计算方法说明
   - SCS单位线法原理
   - 核心公式
```

### 输出文件清单

| 文件类型 | 文件名 | 用途 |
|---------|--------|------|
| Word报告 | Flood_Hydrograph_Report.docx | 完整分析报告（含图表） |
| Excel数据 | Flood_Hydrograph_Report.xlsx | 原始数据+过程线图表 |
| HTML图表 | precipitation_runoff_chart.html | 交互式可视化 |
| PNG图片 | precipitation_runoff_chart.png | 静态图表（打印用） |

## 工作流程

### 步骤1：数据接收与验证

确认以下输入：
- 流域面积 A (km²)
- 逐时段降雨数据 (mm/h)
- 径流系数或CN值
- 其他可选参数（汇流时间、滞后时间等）

### 步骤2：计算净雨量

```
净雨量 = 总降雨 × 径流系数
```

### 步骤3：汇流参数计算

1. 确定CN值（根据流域特征）
2. 计算汇流时间tc
3. 确定滞后时间tp
4. 计算洪峰流量Qp

### 步骤4：推算洪水过程线

使用SCS单位线法进行卷积计算。

### 步骤5：生成可视化图表

按照上述"可视化图表标准"生成：
- 交互式HTML图表（用于展示）
- 静态PNG图片（用于打印）

### 步骤6：输出完整报告

生成Word报告，包含：
- 参数表格
- 数据表格
- 静态图表
- 分析结论

## 注意事项

1. **可视化关键点**
   - 净雨量柱必须叠在总降雨柱**内部**，不是并排
   - X轴必须在顶部（降雨柱向下倒置）
   - 流量曲线从基线向上延伸

2. **报告质量**
   - 表格必须美观专业（渐变表头、条纹行）
   - 洪峰数据必须高亮显示
   - 静态图表必须适合打印（高dpi）

3. **计算准确性**
   - 验证洪峰流量量级是否合理
   - 检查洪水总量是否等于总净雨量×面积
   - 确认峰现时间滞后于降雨峰值

## 技术实现参考

详见 `scripts/` 目录下的参考脚本：
- `scs_unit_hydrograph.py`: SCS单位线法计算核心

`generate_chart.py` 和 `generate_report.py` 在当前仓库中尚未实现；
`references/visualization_standards.md` 仅提供图表规范，不承诺已有导出器。

## 更新记录

- 2026-03-27: 初始版本，包含SCS单位线法、专业可视化标准、完整报告模板
