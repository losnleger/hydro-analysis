---
name: hydro-analysis
description: 面向 WorkBuddy 等 Agent 开发与运行的水文分析 skill。用于有证据边界的降雨-径流推算、SCS-CN/Green-Ampt/Horton 产流、NRCS 单位线、基流、河道与水库汇流、多模型场景筛选，以及水文可视化和报告输出。当用户提到“洪水过程线”“降雨径流”“产流模型”“单位线”“Muskingum”“水库调洪”“水文分析”或“汇流计算”时使用此技能。
---

# 面向 Agent 的水文分析专业技能 (Hydro-Analysis Skill)

## 概述

这是一个面向 WorkBuddy 等 Agent 开发与运行的 skill，用于描述和复用水文分析工作流，包括：

- 降雨损失/产流计算（SCS-CN、Green-Ampt、Horton）；
- 单位线与转换（NRCS PRF=100–600、S 曲线任意历时转换）；
- 基流、河道汇流与水库调洪；
- 场景适用性筛选、候选模型组合与实测指标比较；
- 显式多事件切分，以及 calibration / validation / blind 数据隔离；
- 专业水文可视化图表规范；
- Word、Excel、HTML 报告结构规范。

计算内核提供分层的产流、转换、基流、河道汇流与水库调洪链条，支持 lag、
linear reservoir、lag-and-K、Muskingum，以及试算法、Modified Puls 和半图解法。
使用 `scripts/full_chain.py` 可运行完整链条，并随包提供 PNG / 单文件离线 HTML /
Excel / Word 四件套报告生成器。模型推荐只表示在当前数据与适用性约束下的候选
优先级，不表示唯一最优模型；单场 NSE/KGE 只能诊断，不得据此改排候选或宣称
“最佳”。多事件工作流按事件等权汇总，禁止拼接所有点后让长事件支配结果。
结构化路径要求降雨和实测流量显式声明带时区时间戳、时段语义、单位、逐点质量、
测站、来源和 QC 接受规则，并把原始/规范化输入 hash 写入结果；缺测、未知字段、
时间歧义或未接受质量必须失败关闭，不得静默填补或猜测。
若河道路由指定 `routing_dt_h`，流量、洪量、峰现和退水时标必须全部沿用该
实际等间隔网格；不得再乘转换层 `dt`。水库缺省继承河道实际步长，显式水库
`dt_h` 只允许与上游网格一致，不一致时失败关闭，不得静默忽略或自动跨语义
重采样。
在 Windows 或其他非 UTF-8 控制台运行命令行时使用 `python -X utf8`，避免中文
帮助与报告状态信息因控制台编码失败。

## 使用场景

当用户需要：
1. 推算出口断面的洪水过程线
2. 进行降雨-径流分析
3. 生成专业水文可视化图表
4. 输出完整的水文分析报告

## 核心计算方法

### SCS-CN 参数选取（专业取值流程）

> CN 值绝不能只按"半干旱 70-80、湿润 80-90"粗略取数。专业取值必须依次
> 确定 **水文土壤组（HSG）→ 前期土壤湿度（AMC）→ 土地利用/覆被 → 水文
> 条件 → 耕作方式** 五要素，再按区域适用性校核。下列取值表以 USDA
> NRCS TR-55（1986）表 2-2a/2-2b/2-2c/2-2d 为依据（AMC II 基准），
> 脚本中对应 `select_cn()` / `adjust_cn_for_amc()` / `composite_cn()`。

#### 第1步：确定水文土壤组（HSG）

NEH-630 第 7 章表 7-1 使用饱和导水率 `Ksat`、不透水/限制层深度、高水位
深度及排水条件综合划分 HSG，并包含 A/D、B/D、C/D 双重组。下表仅是
“无不透水层和高水位、评价 0–50 cm 最低透水层”这一行的筛查值：

| 组 | 筛查 Ksat (μm/s) | 对应 in/h | 产流潜力 |
|----|------------------|-----------|----------|
| A | > 40 | > 5.67 | 低 |
| B | > 10 至 ≤ 40 | > 1.42 至 ≤ 5.67 | 中等 |
| C | > 1 至 ≤ 10 | > 0.14 至 ≤ 1.42 | 较高 |
| D | ≤ 1 | ≤ 0.14 | 高 |

> 不得仅凭中国土壤质地、旧“最小下渗率”阈值或经验印象直接确定 HSG。
> 缺少土壤剖面、Ksat、限制层和地下水位资料时，应向用户索取或明确标为假设。

#### 第2步：确定前期土壤湿度条件（AMC）

按前 5 日累计雨量分级（TR-55 表 2-1）：

| AMC 等级 | 前5日雨量（休闲期） | 前5日雨量（生长期） | 含义 |
|----------|--------------------|--------------------|------|
| I（干） | < 13 mm | < 36 mm | 土壤干旱，产流小 |
| II（平均） | 13 – 28 mm | 36 – 53 mm | **默认基准，取值表均为此级** |
| III（湿） | > 28 mm | > 53 mm | 土壤近饱和，产流大 |

AMC I/III 换算（NEH-630 第 10 章，脚本 `adjust_cn_for_amc()`）：

```
CN(I)   = 4.2 × CN(II) / (10 − 0.058 × CN(II))
CN(III) = 23  × CN(II) / (10 + 0.13  × CN(II))
```

#### 第3步：按土地利用查 CN 取值表（AMC II 基准）

**表A：耕作农地（TR-55 表 2-2b）**，值为 (A, B, C, D) 四组土壤的 CN：

| 土地利用 | 耕作方式 | 水文条件差 | 水文条件好 |
|----------|----------|-----------|-----------|
| 休耕地（裸地） | 直行 | 77, 86, 91, 94 | — |
| 中耕作物（玉米/大豆/旱地等） | 直行 | 72, 81, 88, 91 | 67, 78, 85, 89 |
| | 等高 | 70, 79, 84, 88 | 65, 75, 82, 86 |
| | 等高+梯田 | 66, 74, 80, 82 | 62, 71, 78, 81 |
| 小粒谷物（小麦等密植谷物） | 直行 | 65, 76, 84, 88 | 63, 75, 83, 87 |
| | 等高 | 63, 74, 82, 85 | 61, 73, 81, 84 |
| | 等高+梯田 | 61, 72, 79, 82 | 59, 70, 78, 81 |
| 密植豆科/轮作牧草 | 直行 | 66, 77, 85, 89 | 58, 72, 81, 85 |
| | 等高 | 64, 75, 83, 85 | 55, 69, 78, 83 |
| | 等高+梯田 | 63, 73, 80, 83 | 51, 67, 77, 80 |

**表B：其他农地（TR-55 表 2-2c）**：

| 土地利用 | 差 | 中 | 好 |
|----------|----|----|----|
| 牧场/草地/放牧地 | 68, 79, 86, 89 | 49, 69, 79, 84 | 39, 61, 74, 80 |
| 割草草地（禁牧） | — | — | 30, 58, 71, 78 |
| 灌木地 | 48, 67, 77, 83 | 35, 56, 70, 77 | 30, 48, 65, 73 |
| 林草组合（果园/苗圃） | 57, 73, 82, 86 | 43, 65, 76, 82 | 32, 58, 72, 79 |
| 林地 | 45, 66, 77, 83 | 36, 60, 73, 79 | 30, 55, 70, 77 |
| 农村庄院（建筑/场院/道路） | — | — | 59, 74, 82, 86 |

**表C：城镇用地（TR-55 表 2-2a）**：

| 土地利用 | (A, B, C, D) |
|----------|--------------|
| 不透水面（屋顶/停车场/铺装+管网） | 98, 98, 98, 98 |
| 商业区（85% 不透水） | 89, 92, 94, 95 |
| 工业区（72% 不透水） | 81, 88, 91, 93 |
| 居住区 ≤1/8 英亩（联排，65%） | 77, 85, 90, 92 |
| 居住区 1/4 英亩（38%） | 61, 75, 83, 87 |
| 居住区 1/3 英亩（30%） | 57, 72, 81, 86 |
| 居住区 1/2 英亩（25%） | 54, 70, 80, 85 |
| 居住区 1 英亩（20%） | 51, 68, 79, 84 |
| 居住区 2 英亩（12%） | 46, 65, 77, 82 |
| 在建开发区（新平整裸地） | 77, 86, 91, 94 |
| 开放空间（草坪/公园）：差/中/好 | 68,79,86,89 / 49,69,79,84 / 39,61,74,80 |
| 铺装道路（明渠排水）/ 砾石路 / 土路 | 83,89,92,93 / 76,85,89,91 / 72,82,87,89 |
| 荒漠城镇（天然/人工景观） | 63,77,85,88 / 96,96,96,96 |

**表D：干旱半干旱牧场（TR-55 表 2-2d）**，多数类型原表只给 A/B/C 三列：

| 土地利用 | 差 | 中 | 好 |
|----------|----|----|----|
| 草本植被（草/杂草/低灌丛） | 80, 87, 93 | 71, 81, 89 | 62, 74, 85 |
| 栎-山杨山地灌丛 | 66, 74, 79 | 48, 57, 63 | 30, 41, 48 |
| 矮松-刺柏林（草下层） | 75, 85, 89 | 58, 73, 80 | 41, 61, 71 |
| 蒿草灌丛（带草下层） | 67, 80, 85 | 51, 63, 70 | 35, 47, 55 |
| 荒漠灌木（盐灌木/木馏油等） | 63, 77, 85, 88 | 55, 72, 81, 86 | 49, 68, 79, 84 |

**中国补充取值（工程假设，非 TR-55 原表，必须本地率定）**：

| 土地利用 | 建议值 (A, B, C, D) | 说明 |
|----------|--------------------|------|
| 水田（旱作期） | 78, 85, 90, 94 | SCS-CN 基于"先蓄满后产流"机制，对长期蓄水稻田适用性差；淹灌期建议按水面/不透水面（CN≈98）或改用蓄满产流模型 |
| 梯田旱地（水平沟/水平阶/反坡台） | 62, 72, 81, 85 | 黄土丘陵区实测表明整地工程显著降低 CN（冯憬等 2021），以当地率定为准 |

#### 第4步：AMC 修正与综合取值

- 单一场次：用第2步公式把 AMC II 基准值换算到实际 AMC 等级；
- 多种土地利用混合流域：面积加权 `CNc = Σ(CNᵢ×Aᵢ)/Σ(Aᵢ)`（脚本 `composite_cn()`）；
- 城镇混合区：当总不透水率 `<30%` 时，可按 NEH-630 Subpart E
  eq. 630E-2 对不连通面积使用 `(1−0.5R)` 折减；当总不透水率 `≥30%`
  时必须切换到 eq. 630E-1 面积加权关系，不再给不连通折减
  （脚本 `composite_cn_urban()` 已按此边界实现）。

#### 第5步：中国区域适用性校核

| 区域 | 年降水 | 取值建议 | 初损率 λ |
|------|--------|----------|---------|
| 湿润区（南方） | > 800 mm | 直接查 TR-55 表（AMC II）；稻田、河网蓄满产流区慎用 SCS-CN | 0.2 |
| 半湿润区（华北/东北南部） | 400 – 800 mm | 直接查表；校核设计雨量与 AMC II 前5日雨量假设 | 0.2 |
| 半干旱区（黄土高原等） | 200 – 400 mm | 查表后必须率定；λ=0.05 只是文献建议，不能与标准 CN 表自动拼接 | 0.05（仅联合率定选项） |
| 干旱区（西北内陆） | < 200 mm | 优先查表 2-2d；非标准 λ 与 CN 必须联合率定 | 0.05（仅联合率定选项） |
| 高寒区（青藏高原） | — | 冻融/冰雪产流机制与 SCS-CN 假设差异大，不推荐使用 | — |

#### 净雨量计算（SCS-CN 产流公式）

```
S  = 25400/CN − 254          （最大潜在滞留量, mm）
Ia = λ × S                    （初损, mm；NRCS CN 表的标准 λ=0.2）
Q  = (P − Ia)² / (P − Ia + S) （P > Ia 时；否则 Q = 0）
```

脚本中对应 `cn_to_s_mm()`、`initial_abstraction_mm()`、`direct_runoff_mm()`。
NEH-630 明确指出：现有 CN 曲线由 `Ia=0.2S` 建立，若改用其他 `Ia/S`
关系，就必须开发/率定新的 CN 集。因此脚本不会根据区域自动套用 0.05；只有
用户显式传入 `lam=0.05` 才运行，并在结果 `assumptions` 中标记联合率定要求。

### NRCS 单位线时间参数

| 参数 | 定义与关系 | 单位/约束 |
|------|------------|-----------|
| Tc | 汇流时间；优先采用经核验的流路分段行程时间 | min |
| lag | watershed lag | `lag = 0.6 Tc`，h |
| ΔD | 单位净雨历时 | NRCS 目标 `ΔD = 0.133 Tc`，实际值须整除输入雨量时段且 `ΔD ≤ 0.25Tp` |
| Tp | 从单位净雨开始到单位线峰值 | 用实际 ΔD 计算 `Tp = ΔD/2 + lag`，h |
| dt | 输出采样步长 | 必须整除实际 ΔD，且 `dt ≤ 0.1Tp`；不能代替单位净雨历时 |

Kirpich 在新版 NEH-630 Subpart F 中为
`Tc=0.007 L_ft^0.77 S^-0.385` min，换成 `L_m` 后系数约 0.0175。
它来自 7 个 Tennessee 农村、沟道明确且坡陡的小流域，面积仅 1.25–112 acre。
脚本要求显式提供 `L(km)` 与 `slope(m/m)`，超出该面积域默认拒绝；不能再用
`L=1.5√A` 静默替代实测水力流路。

### 洪峰流量计算公式

```
qp = 0.208333 × A × Q / Tp
```
其中：
- qp: 单位线理论洪峰流量 (m³/s)
- A: 流域面积 (km²)
- Q: 单位净雨深 (mm)
- Tp: 单位线峰现时间 (h)

`Q=1 mm` 时系数为 0.208333；`Q=10 mm` 时等价于旧写法
`Qp=2.08333A/Tp`。脚本保留 `Qp` 作为 10 mm 单位线理论峰值，另返回
`Qp_per_mm`。本场洪峰必须读取 `peak_flow`，不能把单位线理论峰值直接当成
任意降雨的洪峰。标准曲线直接插值 NEH-630 第 16 章表 16-1，完整延伸至
`5Tp`；不再使用自定义幂指数曲线或事后强制缩放洪峰。

离散卷积会检查 `Σq·dt = ΣPe·A`。这是代码路径的质量平衡恒等式，只证明
单位和离散实现一致，不代表真实流域率定、预测精度或专业验收。

## 可视化图表标准

### 专业降雨—径流组合图（强制契约）

默认采用水文常用的同窗组合图：时间轴置顶，左轴降雨向下为正，右轴流量向上
为正。总降雨为蓝色宽柱，净雨为橙色窄柱；两柱使用同一时段中心和同一零线，
净雨柱仅在水平方向居中覆盖于总雨柱内部，绝不并排、堆叠或从总雨柱底部起画。
流量过程线使用相同的连续时间坐标。事件产流/单位线过程必须在雨量结束后保留
完整单位线退水段；若后接水库调洪，图表还必须报告调洪计算时域终点的库容和
下泄。未延算到明确工程终止标准时，不得把上游入流时域终点称为水库完整退水。

#### 数据与时间映射

必须先调用 `prepare_precipitation_runoff_plot_data(result)`。默认雨量轴为
`mm/h`：总雨深和净雨深同时除以实际 ΔD；如改用时段雨深，两者必须同时保持
`mm/ΔD`。严禁把总雨强 `mm/h` 与净雨深 `mm` 画在同一雨量轴。

- 雨柱中心：`t_start + ΔD/2`
- 总雨柱宽：`0.80ΔD`；净雨柱宽：`0.45ΔD`
- 流量横坐标：原始 `time_h`，不得按雨量类别索引重采样
- 必须验证同一 ΔD 内 `0 ≤ 净雨 ≤ 总雨`
- `time_h=0` 表示首个雨量时段起点；图题或说明中保留这一时标约定

完整链四件套按原始雨量时段展示时，必须先把 ΔD 净雨深聚合到实际
`dt_rain_h` 并换算为同单位雨强；柱中心和柱宽也使用真实 `dt_rain_h`，禁止
把 0.5 h、2 h 等输入静默当作 1 h。

#### 几何与视觉规则

| 要素 | 强制规则 |
|------|----------|
| X轴 | 单一共享连续时间轴，置顶并显示“时间 (h)”；若复制底轴，两轴范围和刻度必须完全绑定 |
| 左Y轴 | 降雨轴倒置，0 位于顶部；标签与数据单位一致 |
| 右Y轴 | 流量 `m³/s`，0 位于底部，向上增大 |
| 总降雨 | `#5B9BD5` 蓝色宽柱，透明度约0.78 |
| 净雨 | `#ED7D31` 橙色窄柱，透明度约0.90，位于总雨柱上层 |
| 流量 | `#27AE60` 绿色实线，默认直线连接、不得平滑；可加0.10–0.15透明填充 |
| 洪峰 | 只在真实 `peak_time_h/peak_flow` 位置标注，不得用平滑曲线重算峰值 |

默认禁用 `smooth/spline/bezier`，因为这类插值可能移动洪峰、产生过冲或负流量。
输出建议 16:9；屏幕不少于 1280×720，打印 PNG 使用 300 dpi。图例应明确
“总降雨、净雨、流量”，并使用边框/线型保证灰度打印仍可区分。

完整的数据映射、内联 SVG/可选本地 ECharts/Matplotlib 实现要求和验收清单见
`references/visualization_standards.md`。

### 表格规范

#### 数据表格样式

| 要素 | 规范 |
|------|------|
| 表头 | 深色渐变背景（#34495e → #2c3e50），白色文字 |
| 时间列 | 浅灰背景（#ecf0f1），加粗 |
| 数据列 | 按类型配色（蓝/橙/绿） |
| 洪峰行 | 黄色背景（#fff3cd），加粗 |
| 悬停效果 | 浅蓝背景（#e8f4fc） |

#### 双列布局

将16个时段数据分为两列展示（0-7时、8-15时），提高可读性。

## 报告输出标准

### Word报告结构

```
1. 流域与降雨参数
   - 流域面积、径流系数、降雨时段
   - 总降雨量、总净雨量

2. 汇流参数
   - CN值、汇流时间、滞后时间
   - 洪峰流量系数

3. 洪水过程线数据
   - 完整时段数据表
   - 洪峰行高亮显示

4. 洪水特征汇总
   - 洪峰流量、峰现时间
   - 洪峰模数、涨退水历时
   - 洪水总量

5. 分析结论
   - 方法适用性说明
   - 洪水过程特征描述
   - 建议

6. 降雨-径流可视化图表
   - 单文件离线图表说明（HTML文件）
   - 静态图表（PNG，适合打印）

附录：计算方法说明
   - SCS单位线法原理
   - 核心公式
```

### 输出文件清单

| 文件类型 | 文件名 | 用途 |
|---------|--------|------|
| Word报告 | Flood_Hydrograph_Report.docx | 完整分析报告（含图表） |
| Excel数据 | Flood_Hydrograph_Report.xlsx | 原始数据+过程线图表 |
| HTML图表 | precipitation_runoff_chart.html | 内联 SVG 单文件离线可视化 |
| PNG图片 | precipitation_runoff_chart.png | 静态图表（打印用） |

## 工作流程

### 步骤1：数据接收与验证

确认以下输入：

- 流域面积 A (km²)
- legacy 路径：等时段雨强 (mm/h) 及每个输入时段长度 `dt_rain_h` (h)
- structured 路径：`rainfall_data` 的带 offset 时间戳、`interval_start` 或
  `interval_end`、雨强/时段雨深及单位、regular 时段、逐点 quality、station、
  source 和 QC 接受规则；完整 schema 见 `references/data_contract.md`
- 多事件任务必须给 `event-dataset 1.0` 的稳定事件 ID、显式 `[start,end)` 边界和
  `calibration/validation/blind` role；边界对齐、阶段锁及失败规则见
  `references/event_workflow.md`，不得静默推断 dry-gap 或事件阈值
- 若输入实测过程线，structured `observed_data` 必须使用绝对时间并与 structured
  rainfall 对齐；不得混用 structured 降雨与 legacy 相对 `observed.time_h`
- 当前场次 CN；或土地利用、HSG、水文条件、耕作方式和 AMC 的完整查表条件
- 经核验的 Tc(min) 或 Tp(h)；仅在符合适用域时才用显式 L+slope 的 Kirpich
- 若只给 Tp，脚本会用 `ΔD≈0.2Tp` 作工程桥接并标记假设；严格复现
  `lag=0.6Tc` 与 `ΔD=0.133Tc` 时必须给 Tc
- 区域/冻融/积雪等适用性；高寒冰雪产流不得使用本实现

### 步骤2：计算净雨量

专业 CN 路径先确定实际 ΔD。默认以 `0.133Tc` 为目标，将其对齐为
`dt_rain_h` 的精确约数并记录调整；若用户显式给出的 `unit_duration_h` 不能
整除输入时段，则失败关闭。只有输入时段雨强时，假定该时段内雨强均匀，按
ΔD 网格计算：

```
N         = dt_rain_h / ΔD                  （必须为正整数）
P_i,j     = rainfall_i × ΔD                 （j=1...N）
P_cum,i,j = Σ P_i,j
Q_cum,i,j = SCS_CN(P_cum,i,j, CN, λ)
Pe_i,j    = Q_cum,i,j − Q_cum,previous
```

只有在没有 CN/土地利用资料且用户接受粗略假设时，才使用
`Pe_i,j = rainfall_i × runoff_coeff × ΔD`。不得把这条比例式描述为
SCS-CN 专业产流。

### 步骤3：汇流参数计算

1. 确定CN值：按"SCS-CN 参数选取"五步流程（HSG → AMC → 查表 →
   AMC 修正/面积加权 → 区域校核）；脚本用 `select_cn()` +
   `adjust_cn_for_amc()` + `composite_cn()`，或 `analyze_flood_hydrograph(
   ..., land_use=..., hsg=..., hydrologic_condition=..., amc=..., tc=...)` 完成
2. 确定 Tc 与 `lag=0.6Tc`，以 `0.133Tc` 为目标选择能整除 `dt_rain_h`
   且不大于 `0.25Tp` 的实际 ΔD，再用实际 ΔD 计算 `Tp=ΔD/2+lag`
3. 选择能整除实际 ΔD 且不大于 `0.1Tp` 的输出采样步长 `dt`
4. 计算并记录单位线理论峰值；实际场次洪峰以卷积结果 `peak_flow` 为准

### 步骤4：推算洪水过程线

用 NEH-630 第 16 章表 16-1 的 PRF=484 曲线插值生成 0–5Tp 完整单位线。
每个 ΔD 净雨增量在其时段起点施加一次，并将同一条 ΔD-duration 单位线按
ΔD 平移；`dt` 只用于采样和输出。必须同时核对非负性、完整退水、峰现时间
单位和质量平衡误差；不得把单位线按每个 `dt` 重复施加，也不得用任意比例
把过程线峰值强制缩放到 `Qp`。

### 步骤5：生成可视化图表

静态图优先调用 `scripts/generate_chart.py` 中的
`generate_precipitation_runoff_chart()`；它内部先执行
`prepare_precipitation_runoff_plot_data()` 并遵循上述强制契约。宿主 Agent
生成 HTML 时也必须复用同一数据契约和 `references/visualization_standards.md`
验收清单：

- 静态 PNG（仓库已提供，打印默认 300 dpi）
- 单文件离线 HTML（默认内联 SVG，不下载或引用 CDN/外部资源）
- 可选 ECharts 仅接受明确存在的本地 UTF-8 JS 文件并内嵌；URL、网络路径或
  不存在的文件必须失败关闭

### 步骤6：输出完整报告

完整链条统一使用 `scripts/full_chain.py`；已有结果可使用
`scripts/export_report.py` 导出。生成器会输出：

- 参数表格
- 数据表格
- 静态图表
- 分析结论

## 注意事项

1. **可视化关键点**
   - 净雨量柱必须叠在总降雨柱**内部**，不是并排
   - 总雨与净雨必须同单位、同 ΔD、同一时段中心，并从顶部同一零线向下
   - 单一连续时间轴必须置顶；流量使用原 `time_h`；单位线过程保留完整退水，
     水库过程若仅到输入时域边界则明确终点状态，不虚称完整退水
   - 流量曲线从底部零线向上，默认不得平滑或重算洪峰

2. **报告质量**
   - 表格必须美观专业（渐变表头、条纹行）
   - 洪峰数据必须高亮显示
   - 静态图表必须适合打印（高dpi）

3. **计算准确性**
   - 验证输入单位、Tc/Tp 定义、时间网格和方法适用域
   - 检查洪水总量是否等于总净雨深×面积，并报告相对误差
   - 确认过程线有限、非负、峰后退水合理；`peak_time` 的单位为小时
   - 明确“软件/解析基准通过”不等于流域率定或专业验收

## 技术实现参考

详见 `scripts/` 目录下的参考脚本：

- `scs_unit_hydrograph.py`: SCS单位线法计算核心
- `data_contract.py`: 严格水文时序 schema、UTC/单位规范化、QC 与输入 hash
- `event_dataset.py`: 显式多事件切分、三阶段隔离、事件等权指标与阶段锁
- `event_workflow.py`: prepare / evaluate / lock 严格 JSON 命令行入口
- `prepare_precipitation_runoff_plot_data()`: 统一雨量单位、ΔD 时段中心、柱宽和流量时轴的绘图数据契约
- `generate_chart.py`: 已验证的专业倒置雨量—径流静态 PNG 生成器
- `references/scientific_method.md`: 官方来源、公式映射、输入单位、失败关闭规则与验证边界
- `references/data_contract.md`: 结构化降雨/实测流量字段、单位、时间和 QC 规则
- `references/event_workflow.md`: v0.3.1 多事件边界、KGE/MAE、阶段锁和验证边界

- `full_chain.py`: 完整链条编排入口（产流→单位线→基流→河道→水库 + 四件套报告）
- `exporters.py` / `build_four_piece.py`: 单文件离线 HTML/Excel/Word/PNG 报告导出器（已提供）
- `references/visualization_standards.md` 同时约束已提供 PNG 和宿主 Agent 的其他输出。

## 更新记录

- 2026-09-02: v0.3.2 时间网格修复——修正独立 `routing_dt_h` 的返回顺序、末
  时段守恒、下游洪量/洪峰/退水时标和水库实际步长；水库独立 `dt_h` 不一致及
  外部入流覆盖字段均失败关闭。默认 70/73 字段和多事件冻结结果保持不变，本地
  候选仅达到 L2 离散守恒与软件回归验证。
- 2026-09-01: v0.3.1 多事件评价升级——新增显式事件切分、率定/验证/盲测三分区
  和逐级模型锁；增加 KGE-2009、KGE-2012、MAE 及退化状态。单场指标不得改变
  物理适用性候选顺序，多事件只做事件等权诊断，不自动率定或选择最佳模型。
- 2026-09-01: v0.3.0 数据契约升级——新增显式时区/时段、雨深/雨强和流量
  单位、逐点质量、测站、来源、QC 与输入 hash；缺测、歧义时间和未知字段失败
  关闭，不插补、不重采样、不猜测时区、不配置通用异常阈值。legacy 70 字段、
  水文公式和冻结数值不变，本地候选只达到 L2 软件/单位/守恒验证。
- 2026-09-01: v0.2.3 运行兼容性升级——保持全部水文公式、schema 与数值
  结果不变，增加 CPython 3.13 Windows x64 干净运行验证、包级署名元数据和
  私有/公开发布隔离门槛；本地候选不构成 GitHub 发布授权。
- 2026-08-25: v0.2.2 输出修复——完整链 HTML 改为内联 SVG 单文件离线图，
  禁止下载或静默写入 CDN；专业图统一为顶部时间轴、左侧倒置降雨轴和右侧
  流量轴，可选 ECharts 仅允许显式本地资源内嵌。
- 2026-03-27: 初始版本，包含SCS单位线法、专业可视化标准、完整报告模板
- 2026-03-27: CN 取值专业化——以 USDA NRCS TR-55 表 2-1/2-2a/2-2b/2-2c/2-2d
  与 NEH-630 第 7/10 章为依据，增加水文土壤组、AMC 分级与换算、分土地利用
  分土壤组 CN 取值表、面积加权与城镇不透水修正、中国区域适用性校核（含
  半干旱区 λ=0.05 与黄土丘陵区整地措施建议）；脚本新增 `select_cn()`、
  `adjust_cn_for_amc()`、`composite_cn()`、`composite_cn_urban()`、
  `cn_to_s_mm()`、`initial_abstraction_mm()`、`direct_runoff_mm()`、
  `initial_abstraction_ratio_for_region()` 等函数并附 REFERENCES 引用块
- 2026-03-27: 专业测试发现并修复——(1) Kirpich 汇流时间单位错误（L 须换算
  为 m、坡度用 m/m、去除多余 ×60）；(2) 单位线"归一化+峰值缩放"导致洪量
  不守恒，新增量纲化单位线（`generate_unit_hydrograph(drainage_area_km2=)`）
  与 SCS 累计降雨法产流专业路径，洪水总量与净雨量×面积严格守恒；(3) 明确
  Qp=2.08×A/tp 为 1 cm 净雨单位线峰值，过程线洪峰由量纲化单位线卷积给出
- 2026-08-14: 对上述实现作官方资料复核并纠偏——自定义单位线实际在 2.5Tp
  达峰，已替换为 NEH-630 第 16 章表 16-1；分离 Tc、lag、ΔD、Tp 与 dt；
  修复雨强未乘输入时段、非整数网格漂移、旧路径漏乘时段、时间字段返回索引等
  问题；进一步按 Chapter 16 的单位时段语义改为每隔 ΔD 平移单位线，禁止把
  ΔD-duration 单位线按数值 `dt` 重复施加；补 HSG/城镇不透水边界和全量输入
  验证。验证升级为官方解析基准与软件数值验证，仍不代表真实流域率定或专业验收。
- 2026-08-14: 依据用户提供的专业降雨—径流参考图强化可视化契约：倒置雨量轴、
  净雨窄柱嵌入总雨宽柱、单一连续时间轴、同单位 ΔD 映射、真实洪峰和禁止失真
  平滑；新增通过几何回归测试的静态 PNG 生成器。
